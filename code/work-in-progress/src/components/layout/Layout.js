import { Fragment } from 'react';
import Header from './Header';

const Layout = () => {
  return (
    <Fragment>
      <Header />
      <main>Layout</main>
    </Fragment>
  );
};

export default Layout;
